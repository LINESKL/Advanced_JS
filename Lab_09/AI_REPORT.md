# AI REPORT
## Lab 09: Behavioral Design Patterns - System Communication
### Student Name: Нурканат Алиар

---

# Lab 9.1: Observer and Pub/Sub Pattern (50 Points)

## 1. Информация об использовании AI
- **Инструмент:** Google Gemini
- **Дата:** 2026-03-12

## 2. Конкретные промпты
- **Промпт 1:** «Полностью обьясню эту лабку» — получил подробный разбор требований и объяснение паттернов.
- **Промпт 2:** «Давай приступим к первой части... обьясни что сделать и почему» — получил план реализации `EventBus` и объяснение важности `decoupling`.
- **Промпт 3:** «Как должны выглядеть эти файлы в 1 таске» — получил шаблоны классов для `EmailNotifier.js`, `PushNotifier.js` и `DashboardWidget.js`.
- **Промпт 4:** «index.html дай для таска» — получил готовый шаблон для тестирования системы.
- **Промпт 5:** Логи ошибок из браузера (MIME-type) — получил помощь в исправлении путей импорта модулей.

## 3. Модификация и проверка
- Самостоятельно реализовал класс `EventBus` с использованием `Map`.
- Самостоятельно распределил логику по папкам `pubsub/` и `subscribers/`.
- Исправил все импорты, добавив расширение `.js`.
- Протестировал работу системы, убедился в корректной работе отписки (`unsubscribe`).

## 4. Что узнал
- Понял механизм работы `EventBus` как централизованного посредника.
- Узнал, как правильно управлять подписками для предотвращения утечек памяти.
- Разобрался с особенностями работы ES-модулей в браузере.

## 5. Процент использования
- **30% (AI):** Объяснение архитектуры, шаблоны классов, помощь с ошибками путей.
- **70% (Сам):** Реализация логики, структура проекта, запуск и отладка.

---

# Lab 9.2: Mediator and Command Pattern (50 Points)

## 1. Информация об использовании AI
- **Инструмент:** Google Gemini
- **Дата:** 2026-03-12

## 2. Конкретные промпты
- **Промпт 1:** «index.html дай для таска» (для 2-й части) — получил структуру демонстрации.
- **Промпт 2:** Проверка файлов (Mediator, Command) — получил ревью корректности реализации паттернов.

## 3. Модификация и проверка
- Самостоятельно реализовал `Device` и конкретные устройства (`Light`, `Thermostat`).
- Самостоятельно написал логику `HomeMediator`.
- Самостоятельно реализовал паттерн «Команда» и `CommandManager` с поддержкой undo/redo.
- Использовал ИИ только для проверки кода на соответствие паттернам и структуры `index.html`.

## 4. Что узнал
- Понял, как медиатор позволяет устройствам взаимодействовать без прямых ссылок друг на друга.
- Освоил паттерн «Команда» как способ инкапсуляции операций для реализации истории действий.
- Научился разделять логику `execute` и `undo`.

## 5. Процент использования
- **10% (AI):** Шаблон демонстрационного `index.html` и ревью кода.
- **90% (Сам):** Проектирование иерархии, написание логики медиатора и менеджера команд, отладка системы.
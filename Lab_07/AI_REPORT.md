# AI REPORT
## Lab 07: Creational Design Patterns
### Student Name: Нурканат Алиар
---

# Lab 7.1: Singleton Pattern (50 Points)

## 1. Информация об использовании AI 
- Инструмент: Google Gemini
- Дата: 2026-02-26

## 2. Конкретные промпты

### Промпт 1:
`Обьясни эту лабороторную работу (прикреплен PDF файл)`
**Что получил:** Подробный разбор требований лабораторной работы, объяснение сути порождающих паттернов Singleton и Factory, а также список строгих требований к сдаче (Git-дисциплина, структура папок).

### Промпт 2:
`ок начнем с 1`
**Что получил:** Готовый код для реализации классового Singleton (`singleton.js`), модульного Singleton (`config.js`) и тестового файла (`test.js`), а также текстовое объяснение паттерна для файла `README.md`.

## 3. Модификация и проверка
- Самостоятельно создал правильную структуру директорий `Lab_07/task1/` и инициализировал Git-репозиторий.
- Перенес предложенный код в соответствующие файлы, проверил логику работы через запуск `node test.js`.
- Убедился, что оператор строгого равенства (`===`) возвращает `true` для разных ссылок на один и тот же инстанс.
- Оформил историю изменений согласно стандарту Conventional Commits (feat, test, docs).

## 4. Что узнал
- Понял, как реализовать Singleton в современном JS с использованием статических свойств класса (`static instance`).
- Узнал, что ES6-модули кэшируются при импорте, что делает их идеальным и самым простым способом создания Singleton "из коробки" (Module-based Singleton).
- Осознал недостатки паттерна, такие как создание глобального состояния и усложнение тестирования.

## 5. Процент использования
- 20% - AI сгенерировал базовый код и помог сформулировать ответы для README.
- 80% - Самостоятельная настройка окружения, запуск тестов, проверка равенства инстансов и ведение Git-истории.

---

# Lab 7.2: Factory Pattern (50 Points)

## 1. Информация об использовании AI
- Инструмент: Google Gemini
- Дата: 2026-02-26

## 2. Конкретные промпты

### Промпт 1:
`ок дальше`
**Что получил:** Шаблоны классов уведомлений (Email, SMS, Push) с общим интерфейсом `send()`, реализацию `NotificationFactory` с использованием `switch/case` и пример использования в `app.js`.

### Промпт 2:
`mak@MacBook-Air-Mak task_2 % node app.js ... TypeError: NotificationFactory.getSupportedTypes is not a function ...` (вставка лога ошибки из терминала)
**Что получил:** Анализ ошибки и объяснение того, что в классе `NotificationFactory` был пропущен модификатор `static` перед методами. AI подсказал, как правильно объявлять статические методы, чтобы вызывать их без `new`.

## 3. Модификация и проверка
- Создал папку `task2/notifications/` и распределил классы по отдельным файлам для соблюдения модульности.
- Исправил ошибку с `TypeError`, добавив ключевое слово `static` к методам `create` и `getSupportedTypes` в фабрике.
- Запустил `node app.js` и убедился, что фабрика корректно создает разные типы уведомлений и успешно выбрасывает ошибку (throw Error) при передаче неизвестного типа (например, "telegram").
- Написал `README.md` с объяснением разницы между Factory и обычным Constructor.

## 4. Что узнал
- Понял главную цель паттерна Factory — делегирование логики создания объектов и избавление основного кода от жесткой привязки к конкретным классам.
- Научился реализовывать полиморфизм в JavaScript: разные классы имеют одинаковый метод `send()`, но разную внутреннюю реализацию и конструкторы.
- Закрепил понимание разницы между методами экземпляра (instance methods) и статическими методами класса (static methods).

## 5. Процент использования
- 25% - AI предоставил архитектуру классов для фабрики и помог с дебаггингом ошибки `static`.
- 75% - Самостоятельный поиск причины ошибки, исправление кода, тестирование обработки исключений (try/catch) и финальная сборка проекта.